{
  "name": "frontend",
  "version": "1.0.0",
  "main": "src/main.js",
  "scripts": {
    "dev": "vite",            // 개발 서버 실행 명령어
    "build": "vite build",    // 빌드 명령어
    "preview": "vite preview" // 빌드 후 로컬에서 미리보기
  },
  "dependencies": {
    "svelte": "^3.0.0",
    "vite": "^4.0.0",
    "@sveltejs/vite-plugin-svelte": "^2.0.0"
  },
  "devDependencies": {
    "vite": "^4.0.0",
    "@sveltejs/vite-plugin-svelte": "^2.0.0"
  }
}
